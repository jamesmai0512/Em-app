<%= form_for(em) do |f| %>
  <% if em.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(em.errors.count, "error") %> prohibited this em from being saved:</h2>

      <ul>
      <% em.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_area :name %>
  </div>

  <div class="field">
    <%= f.label :department %>
    <%= f.text_area :department %>
  </div>

  <div class="field">
    <%= f.label :price %>
    <%= f.text_field :price %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
